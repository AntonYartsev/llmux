# Server
HOST=0.0.0.0
PORT=8888
AUTH_PASSWORD=

# Gemini (choose one)
GEMINI_CREDENTIALS=
GOOGLE_CLOUD_PROJECT=

# Claude (choose one)
CLAUDE_CREDENTIALS=

# Fallback chains (format: model1:fallback1,fallback2;model2:fallback3)
FALLBACK_CHAINS=

# Routing overrides (format: model:backend,model:backend)
MODEL_BACKEND_MAP=

# TLS (optional)
TLS_CERT_FILE=
TLS_KEY_FILE=

# Logging
LOG_LEVEL=info
LOG_FILE=
